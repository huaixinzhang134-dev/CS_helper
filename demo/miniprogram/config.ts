/**
 * 自建服务器版配置 —— 连接本地 Express + MySQL 后端
 *
 * 启动后端：
 *   cd server && npm start
 *
 * 微信开发者工具 → 详情 → 本地设置 → 勾选"不校验合法域名"
 */
export const API_BASE = 'http://localhost:3000/api';
export const STATIC_BASE = 'http://localhost:3000';
