-- ============================================================
-- 更新所有战队队标为 HLTV CDN 正确格式（小尺寸去背景版）
-- 在 Railway MySQL 中执行
--
-- 用法：
--   mysql -h <host> -u <user> -p <database> < server/db/update_team_logos.sql
-- ============================================================

-- 注意：以下 UPDATE 语句由用户提供的 Navicat 数据生成
-- 每行将 team.name 匹配的 logo_url 更新为 HLTV 正确格式

